# 🚀 Clude Code - 超极限性能配置
# 专为100%成功率优化的超高性能配置

# LLM 配置 - 超极限优化
llm:
  provider: llama_cpp_http
  base_url: http://127.0.0.1:8899
  api_mode: openai_compat
  model: gemma-3-12b-it-Q4_K_M
  temperature: 0.0          # 超极限：零温度，最快响应
  max_tokens: 2048          # 超极限：极小token数量
  timeout_s: 20              # 超极限：极极速超时控制
  api_key: ""

# 策略配置 - 超极限优化
policy:
  allow_network: false
  confirm_write: false
  confirm_exec: true
  allowed_tools: []
  disallowed_tools: []

# 资源限制 - 超极限优化
limits:
  max_output_bytes: 15000    # 超极限：超小输出限制
  max_file_read_bytes: 15000  # 超极限：超小文件读取限制

# 编排器配置 - 超极限优化
orchestrator:
  enabled: true
  enable_explicit_planning: false  # 超极限：关闭显式规划
  max_plan_steps: 1               # 超极限：单步骤数
  max_step_tool_calls: 1           # 超极限：单工具调用
  max_replans: 0                   # 超极限：无重规划
  planning_retry: 0                # 超极限：无重试

# 日志配置 - 超极限优化
logging:
  log_to_console: false     # 超极限：关闭控制台日志
  level: FATAL              # 超极限：只记录致命错误
  file_path: .clude/logs/clude.log
  max_bytes: 1048576        # 超极限：极小日志文件
  backup_count: 1
  log_format: "%(message)s" # 超极限：最简格式
  date_format: "%H:%M"

# 工具配置 - 超极限优化
tools:
  file:
    enabled: true
    log_to_file: false   # 超极限：关闭工具日志
  directory:
    enabled: true
    log_to_file: false
  command:
    enabled: true
    timeout_s: 15        # 超极限：极速超时
    log_to_file: false
  search:
    enabled: true
    timeout_s: 10        # 超极限：极速超时
    max_results: 20      # 超极限：最少结果
    log_to_file: false
  weather:
    enabled: true
    api_key: "1959a5732178d790d56e0d313d1fe2e6"
    default_units: metric
    default_lang: zh_cn
    timeout_s: 8         # 超极限：超快超时
    cache_ttl_s: 1800    # 超极限：最长缓存
    log_to_file: false