---
title: "Plan Parse Retry"
version: "1.0.0"
layer: "task"
---

上一次输出无法解析为 Plan JSON。
请重新输出“严格 JSON”的 FullPlan（不要解释、不要调用工具、不要输出 ```）。

必须包含字段：type,title,steps,verification_policy
其中：type 必须为 "FullPlan"。


